{% extends 'base.html.twig' %}

{% block body %}
    <div class="card p-3">
        <div class="card-body">
            {# Formularz dodawania aukcji #}
            {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
        </div>
    </div>
{% endblock %}
