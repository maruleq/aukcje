{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
    <h1>{{ auction.title }} ✅</h1>
    <p>{{ auction.description }}</p>
    <p>{{ auction.price }}</p>
    
    <a href="{{ url('auction_index') }}">Powrót</a>
</div>
    
{% endblock %}
